//To use exported module we need to import module exported in global object module.exports

const logger = require('../create module/logger')

logger.log("hello")
    //Now we can use module exported from another file by importing in another file